# From a granularity
forEach(cells):

  sheet:

    forEach(inputSets):

      table:
        columns:
          value:
            label: Valeur
            path: value
          uncertainty:
            label: Incertitude
            path: uncertainty
        lines:
          path: inputs
